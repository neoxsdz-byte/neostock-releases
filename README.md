# NeoStock releases

The Windows installers for NeoStock, published here so that installed copies
update themselves.

Each release carries `NeoStock-Setup-<version>.exe` and `latest.yml`; the app
reads the latter to learn whether a newer version exists. The source code is
not here.
